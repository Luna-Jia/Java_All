package objectsAndClass;

public class Count {

    public int count; // data field

    public Count(int c) { // Constructor with one parameter // Constructor can be overloaded
        count = c;
    }

    public Count () { // Constructor without parameter
        count = 1;
    }
}
